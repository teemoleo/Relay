# Default (omit this): Vite dev/preview proxy forwards /api → http://127.0.0.1:8000
# If you open the built app without that proxy, use your API origin and add CORS on FastAPI:
# VITE_API_BASE_URL=http://127.0.0.1:8000
